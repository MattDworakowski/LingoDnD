.DS_Store
node_modules/
*.log

# secrets — never commit
.env
.env.local
.env.*.local

# expo / build artifacts
.expo/
dist/
web-build/

# macOS/iCloud duplicate copies (e.g. "cover 2.png") — never commit
* [0-9].*
